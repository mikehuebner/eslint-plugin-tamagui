/**
 * The goal of the rule is to enforce
 * certain properties over others. For example, if someone
 * wants to update from using the `space` prop to `gap`, we
 * can enforce that `gap` comes before `space`.
 */
